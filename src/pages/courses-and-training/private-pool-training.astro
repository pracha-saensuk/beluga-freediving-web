---
import Layout from '~/layouts/PageLayout.astro';
import CourseDetailSection from '~/components/sections/CourseDetailSection.astro';
import type { CourseDetail, MetaData } from '~/types';
import BackToLineButton from '~/components/ui/BackToLineButton.astro';

const metadata: MetaData = {
  title: 'Private Pool Training',
  description: 'สำหรับผู้ที่ต้องการซ้อม Reskill หลังไม่ได้ Freedive มานาน หรือ ผู้ที่ต้องการพัฒนาความสามารถให้ดำได้ไกลขึ้นลึกขึ้น และ ปรับเทคนิค',
  robots:{
    index: true,
    follow: true,
  },
};
const courseDetail:CourseDetail = {
  title: 'Private Pool Training',
  subtitle: 'ซ้อมสระแบบ Private', 
  description: 'สำหรับผู้ที่ต้องการซ้อม Reskill หลังไม่ได้ Freedive มานาน หรือ ผู้ที่ต้องการพัฒนาความสามารถให้ดำได้ไกลขึ้นลึกขึ้น และ ปรับเทคนิค',
  courseContentText: 'สิ่งที่ช่วยเทรนได้',
  detailList:[
    {listItems: ['CO2 Tolerance','Hypoxia Tolerance','Finning Technique','No Fins Technique','Lactic Training','ฝึกให้ Dynamics ได้ไกลขึ้น','ฝึกให้ Static ได้นานขึ้น']},
  ],
  imageSrc: '~/assets/images/Try-Freedive.svg',
  }
---

<Layout metadata={metadata}>
  <BackToLineButton/>
  <CourseDetailSection {...courseDetail} />
  <BackToLineButton/>
</Layout>
